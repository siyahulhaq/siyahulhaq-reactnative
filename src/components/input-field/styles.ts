import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  input: {
    borderWidth: 1,
    borderColor: '#000000',
    borderRadius: 10,
    padding: 10,
  },
  error: {
    color: 'red',
    fontSize: 10,
  },
});

export default styles;
